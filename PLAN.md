# מערכת ניהול לעסק חשמלאות — ארכיטקטורה ותוכנית עבודה שלבית

## Context (רקע)

איתמר פותח עסק חשמלאות ורוצה מערכת אחת מסונכרנת שתשרת אותו בשלוש חזיתות:
תוכנת דסקטופ מותקנת (לא רק אתר בדפדפן — אייקון על השולחן, נפתחת מ-Start), אפליקציית
מובייל לשימוש בשטח, ואתר נחיתה שיווקי. כל הנתונים (לקוחות, מלאי, מחירון, הצעות מחיר,
חשבוניות, מסמכים בירוקרטיים, שיווק) צריכים לחיות במקום אחד ולהיות מסונכרנים בזמן אמת
בין כל הממשקים. המערכת היא לעסק שלו בלבד (לא SaaS לחשמלאים אחרים), עם הפרדה בין
כניסת מנהל (גישה מלאה) לכניסת לקוח (פורטל מצומצם — התקדמות עבודה, הצעות מחיר,
חשבוניות). איתמר ביקש לבנות בהדרגה אבל שה-MVP הראשון יכלול את **כל** המחלקות ברמה
בסיסית ועובדת, ולא להעמיק במחלקה אחת ולדלג על השאר. הפרויקט ייבנה על פני מפגשים
רבים עם Claude, ולכן חייב חוסן: קוד שנשמר ב-Git/GitHub ולא נעלם בין שיחות.

הפרויקט מתחיל מאפס (תיקיית העבודה ריקה כרגע) — זו תוכנית לבניה מהיסוד.

## החלטות שכבר סוכמו עם המשתמש

- שימוש יחיד לעסק שלו — לא multi-tenant.
- חשבוניות: כרגע "יש חשבונית" בחוץ ל-חשבוניות מס רשמיות; המערכת עוקבת פנימית בלבד
  (סטטוס, סכום, קישור לחשבונית החיצונית) עם מקום מוכן להרחבה לאינטגרציה רשמית בעתיד.
- טכנולוגיה: איתמר בחר "מה שהכי טוב" — הומלץ סטאק סטנדרטי ומתועד היטב שמתאים לפיתוח
  עם סוכן AI על פני הרבה שיחות.
- דסקטופ: תוכנה **מותקנת אמיתית** (אייקון, Start menu), Windows בעדיפות ראשונה.
- שרת/דאטהבייס: Supabase (Postgres + Auth + Realtime + Storage) — איתמר יפתח חשבון
  חינמי כשנגיע לזה.
- ייבוא נתונים: יש נתונים חלקיים קיימים (אקסלים וכו') לייבא בהמשך — הסכמה תוכנן כך
  שלא תפריע לייבוא (שדות אופציונליים, לא נוקשה מדי).
- סדר עבודה: שלב-שלב, אבל ה-MVP כולל את כל המחלקות ברמה בסיסית (לא רק 1-2 לעומק).
- קוד נשמר ב-GitHub (יש לאיתמר חשבון) — ריפו יוקם בשלב 0.
- **פורטל לקוח: הרשמה והתחברות רגילה (self-signup)**, לא רק חשבון שמנהל יוצר ידנית.
  הפאנל של הלקוח יציג: התקדמות עבודה, חשבוניות, הצעות מחיר, ועוד. פאנל המנהל: ניהול
  לקוחות, ניהול מלאי, וכו'.
- מערכת הפעלה לדסקטופ: Windows (macOS לא בעדיפות כרגע).
- שם העסק: טרם נמסר — ייקבע בתחילת שלב 0 (ריפו/מיתוג ישתמשו בשם זמני placeholder עד אז).

## סטאק טכנולוגי מומלץ

| תחום | בחירה | למה |
|---|---|---|
| שפה | TypeScript בכל מקום (כולל Edge Functions) | שפה אחת בכל האפליקציות מקטינה context-switching לסוכן AI שעובד לאורך הרבה שיחות. |
| Backend | **Supabase** (Postgres + Auth + Realtime + Storage + Edge Functions) | נתונים יחסיים (לקוחות/מלאי/הצעות/חשבוניות) מתאימים ל-Postgres, לא ל-DB מסמכים. RLS אוכף הפרדת מנהל/לקוח ברמת ה-DB. Realtime נותן סנכרון חי בין דסקטופ/מובייל/אתר בלי שרת נפרד לתחזק. |
| אפליקציית ווב/מנהל | **Vite + React + TypeScript SPA** (לא Next.js) | האפליקציה מאחורי login, אין צורך ב-SEO/SSR. build סטטי הוא בדיוק מה ש-Electron אורז. |
| עטיפת דסקטופ | **Electron + electron-builder** | טכנולוגיית JS/npm טהורה (בלי צורך בטולצ'יין Rust בסביבת ענן ארעית), מתועדת מאוד, ומייצרת installer ל-Windows (NSIS) עם קיצור ל-Start Menu ולשולחן העבודה — בדיוק מה שאיתמר ביקש. |
| מובייל | **React Native + Expo** (EAS Build) | הסטנדרט המתועד ביותר, תומך במצלמה (תמונות עבודה), התראות, ועדכוני OTA — מתאים לשימוש בשטח. |
| אתר נחיתה | **Next.js** נפרד | דורש SEO וטעינה מהירה — נפרד לגמרי מהאפליקציה המאובטחת. |
| מונורפו | **pnpm workspaces + Turborepo** | קל להבנה ותחזוקה לצוות של מפתח יחיד (+ AI). |
| עיצוב/UI | **Tailwind CSS + shadcn/ui** (ווב/דסקטופ), **NativeWind** (מובייל) | נגיש, פופולרי, קל לעצב מחדש כשיהיה מיתוג/לוגו. RTL דרך `dir="rtl"` + מחלקות לוגיות (`ms-`/`me-` וכו'). פונט עברי כמו Rubik/Assistant כברירת מחדל זמנית. |
| טפסים/ולידציה | **React Hook Form + Zod** | סכמות Zod משותפות בין ווב/מובייל/ולידציית ייבוא CSV עתידית. |
| ניהול state/סנכרון | **TanStack Query + Supabase Realtime** | מרבית ה-state הוא נתוני שרת — React Query הוא הסטנדרט; Realtime מעדכן את המטמון אוטומטית בין חלונות/מכשירים. |
| לוגיקת שרת רגישה | **Supabase Edge Functions** | להזמנות/שליחת מיילים/אינטגרציית חשבונית עתידית — כל מה שדורש service-role key. |

## מבנה מונורפו

```
apps/
  web/       # SPA ראשי: פאנל מנהל + פורטל לקוח (זהה בסיס קוד, ניתוב לפי role)
  desktop/   # עטיפת Electron סביב build של apps/web, ללא לוגיקה עסקית
  mobile/    # אפליקציית Expo לשטח
  marketing/ # אתר נחיתה Next.js, נפרד לגמרי
packages/
  shared/    # לקוח Supabase, טיפוסי DB, סכמות Zod, קבועים, מחרוזות UI בעברית
  ui/        # רכיבי shadcn/ui משותפים (ווב/דסקטופ)
  config/    # eslint/tsconfig/tailwind משותפים
supabase/
  migrations/  # SQL — מקור האמת לסכמה
  functions/   # Edge Functions
  seed.sql
.github/workflows/  # CI: lint, typecheck, build
```

## מודל נתונים (תמצית)

טבלאות ליבה ב-Postgres, כולן עם `id uuid`, `created_at`, RLS מופעל על כולן:

- **profiles** — 1:1 עם `auth.users`, `role enum('admin','customer','technician')` (technician מוכן לעתיד).
- **customers** — פרטי לקוח; `profile_id` מקושר כשיש התחברות פורטל.
- **jobs** — עבודות: סטטוס, לקוח, הצעת מחיר מקושרת, טכנאי משויך, תאריכים.
- **inventory_items** + **inventory_transactions** — מלאי + יומן שינויים (מקושר לעבודות).
- **price_list_items** — מחירון ציוד.
- **quotes** + **quote_line_items** — הצעות מחיר עם שורות פריטים.
- **invoices** — מעקב פנימי בלבד, עם שדות `external_provider`/`external_reference`/`external_url` מוכנים לאינטגרציה עתידית עם יש חשבונית.
- **documents** — מסמכי בירוקרטיה/תאימות (סוג חופשי כרגע, קובץ ב-Storage, `visible_to_customer`).
- **leads** — שיווק בסיסי, עם המרה ל-customer.
- **app_settings** — key/value (שיעור מע"מ, prefix למספור וכו') — לא הארדקוד.

**הרשמת לקוחות (self-signup):** לקוח נרשם בעצמו (אימייל/סיסמה). ביצירת ה-`profile`
(role='customer') המערכת מנסה להתאים אוטומטית לרשומת `customers` קיימת לפי טלפון/אימייל
(שהמנהל כבר הזין); אם אין התאמה — נוצרת רשומת `customers` חדשה במצב "ממתין לאימות
מנהל" שהמנהל מאשר/ממזג ידנית. כך יש הרשמה חופשית אך בלי לאבד שליטה על זהות הלקוח.

**RLS:** מדיניות "מנהל: גישה מלאה" על כל טבלה דרך helper `is_admin()`; טבלאות ניהוליות
טהורות (מלאי, מחירון, לידים) פשוט **לא** מקבלות מדיניות ללקוח בכלל (ברירת מחדל = חסום).
מדיניות ללקוח: SELECT בלבד על הרשומות שלו עצמו (`customer_id = my_customer_id()`),
כולל documents רק כש-`visible_to_customer = true`.

## תוכנית שלבים

### שלב 0 — יסודות
הקמת ריפו GitHub פרטי, שלד המונורפו, פרויקט Supabase, מיגרציית SQL ראשונית (טבלאות +
enum + RLS בסיסי + trigger ליצירת profile בהרשמה), `packages/shared` עם לקוח
Supabase וטיפוסים, שלד `apps/web` עם RTL, ניתוב מוגן לפי role, login/signup/reset,
ועמוד stub לכל מחלקה. CI בסיסי (lint/typecheck/build).
**קריטריון יציאה:** אפשר להתחבר כמנהל ולראות את כל המחלקות; הרשמת לקוח נכנסת לפורטל מצומצם.

### שלב 1 — MVP: כל המחלקות ברוחב
CRUD אמיתי (לא רק stub) בכל מחלקה, עם הקשרים ביניהן:
לקוחות (רשימה/חיפוש/פרטי לקוח עם טאבים) · מלאי (כרטיסי פריט, התאמות מלאי) · מחירון
(פריטים, נבחר בבניית הצעות) · הצעות מחיר (בונה עם שורות מהמחירון, חישוב מע"מ מ-
app_settings, סטטוסים, הפיכה לעבודה) · עבודות (סטטוסים, שיוך טכנאי, רישום צריכת מלאי)
· חשבוניות פנימיות (סטטוס, קישור ל"יש חשבונית") · מסמכי בירוקרטיה (יצירה, העלאת קובץ,
נראות ללקוח) · לידים/שיווק (יצירה, המרה ללקוח) · פורטל לקוח מלא: התקדמות עבודה, הצעות
מחיר, חשבוניות (read-only, RLS אוכף). Realtime על עבודות/הצעות כדי להוכיח סנכרון חי.
פריסה כאתר רגיל (למשל Vercel) לצורך בדיקה לפני עטיפת דסקטופ/מובייל.
**קריטריון יציאה:** תרחיש קצה-לקצה מלא כמנהל + לקוח רואה רק את שלו.

### שלב 2 — אריזת דסקטופ + העמקה
`apps/desktop` עם Electron + electron-builder → installer ל-Windows עם אייקון וקיצור
ל-Start Menu. העמקה: PDF להצעות מחיר, שליחת הצעה במייל (Edge Function), דשבורד מלאי
נמוך, דוח בסיסי (עבודות החודש, צנרת הצעות, חשבוניות באיחור).
**קריטריון יציאה:** installer שמתקין תוכנה אמיתית עם אייקון, מסונכרנת עם הווב.

### שלב 3 — אפליקציית מובייל לשטח
`apps/mobile` (Expo): רשימת עבודות משויכות, פרטי עבודה עם עדכון סטטוס, רישום צריכת
חומרים, פרטי לקוח עם חיוג/ניווט, צילום ואישור תמונות ל-Storage. הפצה ראשונית דרך
TestFlight/APK פנימי.
**קריטריון יציאה:** טכנאי מעדכן עבודה בנייד ורואים את זה מיידית בדסקטופ/ווב.

### שלב 4 — אתר נחיתה שיווקי
`apps/marketing` (Next.js): עמוד שיווקי מעוצב, RTL, טופס יצירת קשר שכותב ל-leads.
עצמאי לגמרי — אפשר להקדים אם רוצים תוצאה גלויה מהר יותר.

### שלב 5+ — העמקה נוספת
אינטגרציה רשמית עם יש חשבונית (או Green Invoice/iCount כגיבוי) דרך Edge Function;
מסמכי בירוקרטיה מובנים לפי טפסים ישראליים ספציפיים (יוגדרו ע"י איתמר); כלי ייבוא
CSV/Excel ללקוחות ומחירון (משתמש באותן סכמות Zod); שיווק מתקדם לפי צורך; אפשרות
לתפקיד "צוות" נוסף בין לקוח למנהל.

## נושאים פתוחים (לא חוסמים, ייסגרו תוך כדי)

1. שם העסק — placeholder עד שיימסר, ישפיע על שם הריפו/אפליקציה/עיצוב האתר.
2. פרטי ריפו GitHub (שם, חשבון אישי/ארגון).
3. יצירת פרויקט Supabase (איזור מומלץ: אירופה, לקרבה לישראל).
4. סוגי מסמכי בירוקרטיה ישראליים מדויקים (אישור חיבור חח"י, תעודות בטיחות וכו').
5. זמינות API רשמי ל"יש חשבונית" — אם אין, ניפול ל-Green Invoice/iCount.
6. שיעור מע"מ נוכחי לזריעת app_settings.

## אימות/בדיקות

בסוף כל שלב: הרצת `pnpm dev` מקומית ובדיקת תרחיש קצה-לקצה ידנית (התחברות מנהל,
יצירת לקוח→הצעה→עבודה→מלאי→מסמך→חשבונית, ואז התחברות לקוח ווידוא שרואה רק את
שלו). CI מריץ lint/typecheck/build על כל push. לפני מעבר לשלב 2 (דסקטופ) — לוודא
שהבנייה של `apps/web` עצמאית ותקינה כ-static build.
